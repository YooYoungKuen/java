package ch06.sec03;   //패키지 선언

public class SportsCar {		//클래스 선언

	//클래스 이름은 앞문자는 대문자로 만드는것이 관례
	//다른단어가 나오면 다른단어의 첫자는 대문자 . Camel 스타일
	
	//변수 이름은 앞문자를 소문자. 다른단어가 나오면 앞글자 대문자.
	//숫자를 포함할 수 있지만 첫문자는 숫자가 될 수 없고 특수문자 중 $,_를 포함할 수 있다
}
class Tire {  //클래스 선언   
	
	//소스파일은 클래스가 아니다 
	//SportsCar 와 Tire 는 각각 컴파일된다. 소스파일은 하나지만 컴파일하니까 bin폴더에 두개의 클래스가 생성되었다.
	//하나의 소스파일안에는 하나만 공개 클래스로 만들 수 있다.
	//두개 생성시 컴파일 에러.
}